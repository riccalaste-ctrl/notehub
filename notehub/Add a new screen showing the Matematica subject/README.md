# Add a new screen showing the Matematica subject

This is a code bundle for Add a new screen showing the Matematica subject. It's based on default Vite project. The original design is available at https://app.flowstep.ai/file?activeFileId=f736c207-673f-44c7-ba8f-5e49217969b4.

## Running the code

Run `npm i` and `npm run setup` to install the dependencies.

Run `npm run dev` to start the development server.
